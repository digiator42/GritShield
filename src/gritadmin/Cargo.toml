[package]
name = "gritadmin"
version = "0.1.0"
edition = "2021"

[dependencies]
gritshield = { path = "../../" }
tokio = { version = "1.0", features = ["full"] }
sea-orm = { version = "1.1.0", features = [
    "runtime-tokio-native-tls",
    "sqlx-sqlite",
    "with-chrono",
] }
maud = "0.25.0"
chrono = "0.4"
